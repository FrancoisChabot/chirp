`expect_exit(0);
`expect_stdout("bound\ndrop\n");

let Token = struct {};

`implement(
    trait=`drop,
    on=Token,
    impl=`Dropness(drop=(this) => `print("drop"))
);

do {
    let t = Token();
    `print("bound");
};
