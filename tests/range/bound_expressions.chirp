`expect_exit(0);

let start = 1 + 1;
let stop = 2 * 4;

`expect_stdout("true\n");
`print(2 ∈ start..stop);
`expect_stdout("false\n");
`print(8 ∈ start..stop);
`expect_stdout("true\n");
`print(8 ∈ start..=stop);
`expect_stdout("false\n");
`print(9 ∈ start..=stop);
