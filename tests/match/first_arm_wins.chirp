// Arms are tested in order; the first arm whose pattern contains the subject wins.
`expect_stdout("first\n");
`expect_interpreter_exit(0);

// Both `any arms match, but the first one should be selected.
`print(match 42 {
    `any => "first",
    `any => "second"
});
