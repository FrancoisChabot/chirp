// Match with set union (∪) in a pattern. The subject is tested for membership
// in the union of the two sets.
`expect_test_failure;
`expect_stdout("special\nspecial\nnormal\n");
`expect_exit(0);

let low = {1, 2, 3};
let high = {98, 99, 100};

let classify(n) = match n {
    low ∪ high => "special",
    `any => "normal"
};

`print(classify(2));
`print(classify(99));
`print(classify(50));
