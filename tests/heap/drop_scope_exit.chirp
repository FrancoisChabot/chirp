`expect_exit(0);
`expect_stdout("drop\n");

let heap = `import("std/heap");
let Token = struct {};

`implement(
    trait=`drop,
    on=Token,
    impl=`Dropness(drop=(this) => `print("drop"))
);

do {
    let p = heap.create(Token());
};
