`expect_exit(0);

let sum = (x : int, y : int) : int => x + y;
let product = (x : int, y : int) : int => x * y;

`expect_stdout("3\n");
`print(sum(1, 2));
`expect_stdout("12\n");
`print(product(3, 4));
