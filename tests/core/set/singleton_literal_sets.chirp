`expect_exit(0);

`expect(1 ∈ 1);
`expect(2 ∉ 1);
`expect(1 ⊆ {1, 2});
`expect(1 ⊂ {1, 2});
`expect({1, 2} ⊄ 1);
`expect(true ⊆ bool);
`expect("" ⊆ {""});
`expect(#ok ⊆ {#ok, #other});
`expect(undecided ⊆ `typeof(undecided));

let constrained : 1 = 1;
`expect_stdout("1\n");
`print(constrained);
