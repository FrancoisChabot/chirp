`expect_exit(0);

let a = `mint();
let same = a;
let b = `mint();
let constrained : `typeof(a) = a;

`expect(a == same);
`expect(a != b);
`expect(`typeof(a) == `typeof(same));
`expect(`typeof(a) != `typeof(b));
`expect(a ∈ `typeof(a));
`expect(a ∉ `typeof(b));
`expect(b ∈ `typeof(b));
`expect(a ∉ `Set);
`expect(`typeof(a) ∈ `type);
`expect(`typeof(a) ∈ `Set);
`expect(constrained == a);
