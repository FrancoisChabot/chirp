`expect_exit(0);
`expect_stdout("drop\nafter\ndrop\n");

let Token = struct {};

`implement(
    trait=`Drop,
    on=Token,
    impl=`interface_of(`Drop)(drop=(this) => `print("drop"))
);

do {
    let mut t = Token();
    t = Token();
    `print("after");
};
