`expect_exit(0);


// Basic natures should not be unique by default
`expect(1 ∉ `Unique);
`expect("" ∉ `Unique);
`expect(true ∉ `Unique);

// The trait itself isn't a unique value
`expect(`Unique ∉ `Unique);
`expect(`Unique ∈ `nature_of(`Unique));
