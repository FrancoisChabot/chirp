`expect_exit(0);

`expect_stdout("--- Type Identities ---\n");
`print("--- Type Identities ---");
`expect(`typeof(true) == bool);
`expect(`typeof(false) == bool);
`expect(`typeof(1) == int);
`expect(`typeof("") == string);
`expect(`typeof(bool) == `type);
`expect(`typeof(int) == `type);
`expect(`typeof(string) == `type);
`expect(`typeof(`type) == `type);

`expect_stdout("--- Membership Relations ---\n");
`print("--- Membership Relations ---");
`expect(true ∈ bool);
`expect(false ∈ bool);
`expect(1 ∈ int);
`expect("" ∈ string);
`expect(bool ∈ `type);
`expect(int ∈ `type);
`expect(string ∈ `type);
`expect(`type ∈ `type);

`expect_stdout("--- Set Capability Relations ---\n");
`print("--- Set Capability Relations ---");
`expect(bool ∈ `Set);
`expect(int ∈ `Set);
`expect(string ∈ `Set);
`expect(`type ∈ `Set);
`expect(`any ∈ `Set);
`expect(`empty ∈ `Set);
`expect(`Set ∈ `Set);
`expect(true ∈ `Set);
`expect(1 ∈ `Set);
`expect("" ∈ `Set);

`expect_stdout("--- Top (any) and Bottom (empty) Relations ---\n");
`print("--- Top (any) and Bottom (empty) Relations ---");
`expect(true ∈ `any);
`expect(1 ∈ `any);
`expect("" ∈ `any);
`expect(`type ∈ `any);
`expect(`any ∈ `any);
`expect(true ∉ `empty);
`expect(1 ∉ `empty);
`expect("" ∉ `empty);
`expect(`type ∉ `empty);
`expect(`empty ∉ `empty);
