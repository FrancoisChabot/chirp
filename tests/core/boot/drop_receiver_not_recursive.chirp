`expect_exit(0);
`expect_stdout("drop\n");

let Token = struct {};

`implement(
    trait=`Drop,
    on=Token,
    impl=`interface(`Drop)(drop=(this) => do {
        `print("drop");
    })
);

do {
    let t = Token();
};
