`expect_exit(1);

let Token = `mint();

`implement(
    trait=`set,
    on=`typeof(Token),
    impl={
        bp = (this, v) => false,
        br = (this, lc) => {false},
        extra = (this) => void
    }
);
