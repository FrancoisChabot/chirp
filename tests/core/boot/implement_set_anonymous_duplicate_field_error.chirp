`expect_exit(1);

let Token = `mint();

`implement(
    trait=`Set,
    on=`typeof(Token),
    impl={
        bp = (this, v) => false,
        bp = (this, v) => true,
        br = (this, lc) => {false}
    }
);
