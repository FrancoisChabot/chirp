`expect_exit(0);

`expect(`interface_of(`Drop) == `interface_of(`Drop));

let Token = struct {};

`expect(Token() ∉ `Drop);

`implement(
    trait=`Drop,
    on=Token,
    impl=`interface_of(`Drop)(drop=(this) => void)
);

`expect(Token() ∈ `Drop);
