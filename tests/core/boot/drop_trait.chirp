`expect_exit(0);

`expect(`interface(`Drop) == `interface(`Drop));

let Token = struct {};

`expect(Token() ∉ `Drop);

`implement(
    trait=`Drop,
    on=Token,
    impl=`interface(`Drop)(drop=(this) => void)
);

`expect(Token() ∈ `Drop);
