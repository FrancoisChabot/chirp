`expect_exit(0);

`expect(`same(`nature_of(true), bool));
`expect(`same(`nature_of(1), int));

let Minted = `mint_finite(2);
`expect(`same(`nature_of(Minted.nature), `nature));
`expect(Minted.values[0] != Minted.values[1]);
`expect(Minted.values[0] ∈ Minted.nature);
