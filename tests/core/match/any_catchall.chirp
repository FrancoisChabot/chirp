// `any is the set of all values. An arm with `any as its pattern always
// matches, serving as the catch-all / default case.
`expect_exit(0);

let x = 99;
`expect_stdout("other\n");
`print(match x {
    1 => "one",
    `any => "other"
});
